params {
  reads = false
  singleEnd = false
  genome = false
  star_index = false
  gtf = false
  stranded = false
  adapter = "$baseDir/assets/no_adapter.txt"
  readlength = 48
  overhang = false
  mismatch = 2
  igenomes_base = 's3://ngi-igenomes/igenomes/'
  outdir = 'results'
}

process.container = 'lifebitai/splicing-pipelines-nf:latest'

// Load igenomes.config by default
includeConfig 'conf/igenomes.config'

profiles {
  docker { docker.enabled = true }
  test { includeConfig 'conf/test.config' }
  sumner { includeConfig 'conf/executors/sumner.config' }
}
